#!/bin/bash

set -e
set -o nounset

docker build --rm -t up-or-not src/upornot

docker rm -f upornot || true

exec docker run -d --restart=unless-stopped \
  --name upornot \
  upornot \
    8.8.8.8 \
    192.168.164.1 \
    192.168.10.1 \
    38.88.204.185
